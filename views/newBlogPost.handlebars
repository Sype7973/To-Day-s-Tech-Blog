{{!-- to create a blogpost handlebar with text area and submit button--}}
<div>
    <form class="newBlogPost-form">
        <div class="field">
            <label class="label" for="title">Title:</label>
            <div class="control">
                <input class="input" type="text" id="blogTitle" />
            </div>
        </div>
        <div class="field">
            <label class="label" for="body">Body:</label>
            <div class="control">
                <textarea class="textarea" id="blogBody"></textarea>
            </div>
        </div>
        <div class="field">
            <div class="control has-text-centered">
                <button class="button is-primary" type="submit" id="newBlogPostbtn">Submit</button>
            </div>
        </div>
    </form>
</div>

<script src="/js/newBlogPost.js"></script>